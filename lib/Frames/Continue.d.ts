import Frame from "./Frame";
export declare class Continue extends Frame {
    /**
     * Build CONTINUE frame
     *
     * @param data
     * @param final
     */
    constructor(data: Uint8Array, final?: boolean);
}
export default Continue;
